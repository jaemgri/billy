<nav class="app-header">

  <%= link_to root_path, class: "text-decoration-none" do %>
    <h2 class="header-title fs-1">
      <%= image_tag "Billy4.png", alt: "Icon", class: "billy-icon" %>
      Billy
    </h2>
  <% end %>


  <div class="header-right">

    <% if user_signed_in? %>

      <%= link_to edit_user_registration_path, class: "user-pill" do %>

        <span class="user-avatar">
          <%= (current_user.name.presence || current_user.email).first.upcase %>
        </span>

        <%= current_user.name.presence || current_user.email.split("@").first %>

      <% end %>


      <%= button_to "Sign Out",
          destroy_user_session_path,
          method: :delete,
          data: { turbo_method: :delete },
          class: "header-btn header-btn-outline" %>


    <% elsif !current_page?(new_user_session_path) %>

      <%= link_to "Log In",
          new_user_session_path,
          class: "header-btn" %>

    <% end %>

  </div>

</nav>
