<h1>My Bills</h1>

<%= link_to "Add a new bill", new_bill_path %>

<% @bills.each do |bill| %>
  <div>
    <h3><%= link_to bill.name, bill_path(bill) %></h3>
    <p>¥<%= bill.amount %></p>
  </div>
<% end %>
